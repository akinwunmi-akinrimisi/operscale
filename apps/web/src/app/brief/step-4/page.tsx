// Step 4 — Brand voice + assets (logo upload to customer-logos bucket).

export const metadata = { title: 'Step 4 — Brand voice — Operscale' };

export default function Step4Page() {
  return (
    <main className="container max-w-xl py-12">
      <p className="text-sm text-muted-foreground">Step 4 of 7</p>
      <h1 className="mt-2 text-2xl font-semibold">Your brand voice</h1>
      {/* TODO(Operscale): tone questions + logo upload to customer-logos */}
    </main>
  );
}
